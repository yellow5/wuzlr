%div{:class => "team #{team} #{side} #{"winner" if team == match.winner} #{"loser" if team == match.loser}"}
  %ul
    - match.players_for(team).each do |p|
      %li= link_to gravatar_image_tag(p.email, :class => 'gravatar'), user_path(p)
  %span.score= match.score_for(team)
  %span.winningisgood
