- @subheader = "Resend unlock instructions"
- semantic_form_for resource_name, resource, :url => unlock_path(resource_name) do |f|
  = f.error_messages
  - f.inputs do
    = f.input :email
  - f.buttons do
    = f.commit_button "Resend unlock instructions"
= render :partial => "shared/devise_links"
