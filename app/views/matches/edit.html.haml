= error_messages_for 'match'
- @header = "Enter score"
.gumph
  %h3 Enter your score
  %p In the fields opposite enter your score.
  %p If you've been Beckham'esque &amp; given someone a royal thrashing, now might be a good time to rub-it-in.
- form_for [@league, @match], :html => {:id => 'edit_match'} do |f|
  #pitch.pitchedit
    #top.results_top
      .wee
        - @match.red_players.each do |player|
          = gravatar_for_with_default player
      = f.text_field :red_score, :size => 1, :maxlength => 2
    %a#submit_score.play_game{:href => "javascript:;"} Submit Score
    #bottom.results_bottom
      = f.text_field :blue_score, :size => 1, :maxlength => 2
      .wee
        - @match.blue_players.each do |player|
          = gravatar_for_with_default player
